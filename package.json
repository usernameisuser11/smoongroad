{
  "name": "malmoa-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "말모아 AAC 보호자·사용자 PWA",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check db.js && node --check public/app.js && node --check public/guardian-home.js && node --check public/connect.js && node --check public/guardian-auth.js && node --check public/sw.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.0",
    "helmet": "^8.3.0",
    "pg": "^8.22.0"
  }
}
